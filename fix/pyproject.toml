[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "similarity-search-fix"
version = "0.1.0"
description = "Fixed comparable AllNLI 70/15/15 training pipeline for the NLP similarity project"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

