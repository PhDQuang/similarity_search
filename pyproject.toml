[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "similarity-search-nlp"
version = "0.1.0"
description = "Semantic document similarity search project using AllNLI"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

